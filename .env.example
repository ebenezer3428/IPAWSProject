
OPENAI_API_KEY="your-openai-api-key-here"
SEARCH_MODEL="gpt-4o-mini"
OPENAI_MODEL="gpt-4o-mini"

OFFLINE_MODE = "0"

# Google (Firebase) authentication for the backend.
# On Google Cloud Run this is optional: the service uses Application Default
# Credentials automatically. For local development, point this at a Firebase
# service account JSON key to enable Google ID token verification.
# FIREBASE_CREDENTIALS="path/to/serviceAccountKey.json"
